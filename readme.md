## Новая версия бота для VK

### Инициализация
Указать параметры в файле `.env`, выполнить команды:
```bash
    make init
    make up
```
Для разработки, вместо `make init` использовать `make init-dev`

### Команды
`./bin/console echo`
`./bin/doctrine migrate`